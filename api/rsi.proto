syntax = "proto3";
import "base.proto";

package rsi;

option go_package = "internal/proto/monitor";

service rsi {
  rpc get_rsi (PeriodLength) returns (RsiData);
}

message PeriodLength {
  string pl = 1;
  base.DebugData debug = 2;
}

message RsiData {
  string rsival = 1;
  base.DebugData debug = 2;
}
